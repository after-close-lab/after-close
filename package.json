{
  "name": "after-close",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.13.0",
  "engines": {
    "node": ">=24.17.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "check": "astro check",
    "build": "pnpm generate:stats && astro check && astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:md": "markdownlint-cli2 \"**/*.md\" \"#node_modules\" \"#dist\"",
    "validate:content": "tsx scripts/validate-content.ts",
    "validate:ownership": "tsx scripts/validate-ownership.ts",
    "validate:policy": "tsx scripts/validate-policy.ts",
    "scan:sensitive": "tsx scripts/scan-sensitive-data.ts",
    "generate:stats": "tsx scripts/generate-stats.ts",
    "validate": "pnpm validate:content && pnpm validate:policy && pnpm scan:sensitive && pnpm lint:md && pnpm check",
    "after-close": "tsx scripts/after-close.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.0.9",
    "yaml": "2.9.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "markdownlint-cli2": "0.23.0",
    "tsx": "4.23.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
